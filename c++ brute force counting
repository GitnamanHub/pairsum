int pairSum(vector<int> &arr, int n, int target){
	int count{0};
 for(int i=0;i<n;i++)
	 {

	 	for(int j=n-1;j>i;j--)
	 	{

			if(arr.at(j) + arr.at(i) == target)
	 		{
	 			count ++;
				break;
			}

	 	}

	 }

	 if(count==0)
   return count-1;
	 else
	 return count;
